# ResyPro

This repository contains the ResyPro demo application and several related Swift packages.

To build the application using the Swift Package Manager run:

```bash
swift build
```

The root `Package.swift` depends on the local packages found under `ResyPro/Packages` and compiles the main app target.
